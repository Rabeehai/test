git --version

git clone https://github.com/Rabeehai/test.git

ls -la

la

git status

git add.

git commit -m "my message"

ssh-keygen -t rsa -b 4096 -C "rabeeh.abouismail@gmail.com"

cat testkey.pub

pbcopy < ./testkey.pub  
xclip < ./testkey.pub
git push




